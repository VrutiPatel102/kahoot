import 'package:flutter/material.dart';

class QueLoadingScreen extends StatelessWidget {
  const QueLoadingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
