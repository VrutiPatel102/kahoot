import 'package:flutter/material.dart';

class ScoreStatusScreen extends StatelessWidget {
  const ScoreStatusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
